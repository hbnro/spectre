#!/usr/bin/env php
<?php

error_reporting(-1);
date_default_timezone_set('America/Mexico_City');

call_user_func(function () use($argv) {
  require dirname(__DIR__).DIRECTORY_SEPARATOR.'vendor'.DIRECTORY_SEPARATOR.'autoload.php';

  \Spectre\Runner::execute($argv);
});
